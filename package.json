{
  "name": "ciclicoscript",
  "version": "0.1.0",
  "private": true,
  "description": "Dashboard de scripts de Cíclico sobre Cloudflare Workers. Script #1: rotador del banner del canal de YouTube.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cron:test": "wrangler dev --test-scheduled",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
